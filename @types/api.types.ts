export type Contacts = {
    avatar: string;
    name: string;
    id: number;
    time: string;
    lastMessage: string;
    roomId:string;
    isActive:boolean;
}
export type User = {
    username: string;
    token: string;
}